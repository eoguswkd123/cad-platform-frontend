# Phase 1.1: CI/CD + DevOps Implementation

> **Version**: 0.0.1
> **Last Updated**: 2025-12-17

---

## Table of Contents (목차)

- [1. Overview (개요)](#1-overview-개요)
- [2. Architecture (아키텍처)](#2-architecture-아키텍처)
- [3. Implementation Checklist (구현 체크리스트)](#3-implementation-checklist-구현-체크리스트)
- [4. Key Implementation Details (핵심 구현 상세)](#4-key-implementation-details-핵심-구현-상세)
- [5. Testing Strategy (테스트 전략)](#5-testing-strategy-테스트-전략)
- [6. Routes & Navigation (라우트 및 네비게이션)](#6-routes--navigation-라우트-및-네비게이션)
- [7. Dependencies & References (의존성 및 참조)](#7-dependencies--references-의존성-및-참조)
- [8. Post-Completion Checklist (완료 후 체크리스트)](#8-post-completion-checklist-완료-후-체크리스트)
- [9. Changelog (변경 이력)](#9-changelog-변경-이력)

---

## Claude Commands

### 이 문서 검증 시 (품질 검증)

```bash
/sc:spec-panel @docs/phases/01-Foundation/1.1_CI_CD_DEVOPS.md
```

### 검증된 문서 기반 구현 시

```bash
/sc:implement @docs/phases/01-Foundation/1.1_CI_CD_DEVOPS.md --validate
```

---

## Phase Progress (진행 상황) ✅ 완료 (100%)

| Sub-Phase | 상태 | 설명                                 |
| --------- | ---- | ------------------------------------ |
| 1.1.1     | ✅   | 기본 CI/CD (lint, type-check, build) |
| 1.1.2     | ✅   | 테스트 CI 통합 (test, coverage)      |
| 1.1.3     | 📋   | 확장 DevOps (배포, 보안, 모니터링)   |

---

## 1. Overview (개요)

### 1.1 목표

프로젝트의 CI/CD 파이프라인을 확립하여 코드 품질을 자동으로 보장하고, 개발 생산성을 높이며, 안정적인 배포 기반을 마련한다.

### 1.2 MVP 범위

| 항목                  | MVP 범위                        | 확장 단계                      |
| --------------------- | ------------------------------- | ------------------------------ |
| **GitHub Actions CI** | lint + type-check + build       | test + coverage + deploy       |
| **Pre-commit Hooks**  | eslint --fix + prettier --write | 추가 검증 도구                 |
| **Pre-push Hooks**    | lint + type-check + build       | + test 추가                    |
| **테스트 자동화**     | Vitest 설정 완료 (로컬)         | CI 파이프라인 통합             |
| **커버리지 리포트**   | 로컬 실행 (70% 임계값)          | CI에서 리포트 생성 및 검증     |
| **배포 자동화**       | -                               | GitHub Actions 배포 워크플로우 |
| **보안 스캔**         | -                               | 의존성 취약점 스캔             |

### 1.3 기술적 제약

- **GitHub Actions**: ubuntu-latest 러너 사용
- **Node.js 버전**: 20.x (LTS)
- **npm ci**: 재현 가능한 빌드를 위해 lock 파일 사용
- **캐싱**: npm 캐시 활성화로 빌드 시간 최적화

### 1.4 성공 기준 (Acceptance Criteria)

| 항목               | 기준                                          | 측정 방법                          |
| ------------------ | --------------------------------------------- | ---------------------------------- |
| CI 빌드 시간       | < 5분                                         | GitHub Actions 실행 시간           |
| Pre-push 검증 시간 | < 30초                                        | 로컬 훅 실행 시간                  |
| 품질 게이트 통과율 | 100%                                          | 모든 체크 통과 시에만 PR 머지 허용 |
| 테스트 커버리지    | MVP: ≥ 20% line, ≥ 50% branch (목표: 70%/60%) | Vitest coverage report             |

---

## 2. Architecture (아키텍처)

### 2.1 CI/CD 파이프라인 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Development Workflow                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐    ┌──────────────┐    ┌──────────────┐              │
│  │  Code    │    │  Pre-commit  │    │   Pre-push   │              │
│  │  Change  │───▶│    Hook      │───▶│     Hook     │              │
│  └──────────┘    │              │    │              │              │
│                  │ • eslint fix │    │ • lint       │              │
│                  │ • prettier   │    │ • type-check │              │
│                  └──────────────┘    │ • build      │              │
│                                      │ • test ✅    │              │
│                                      └──────┬───────┘              │
│                                             │                       │
│                                             ▼                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    GitHub Actions CI                          │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  Triggers: push/PR → master, develop                          │  │
│  │                                                                │  │
│  │  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐       │  │
│  │  │Checkout │──▶│ Setup   │──▶│  npm ci │──▶│  Type   │       │  │
│  │  │   v4    │   │Node 20  │   │         │   │ Check   │       │  │
│  │  └─────────┘   └─────────┘   └─────────┘   └────┬────┘       │  │
│  │                                                  │            │  │
│  │                                                  ▼            │  │
│  │  ┌─────────────────────────────────────────────────────┐     │  │
│  │  │  ┌─────────┐   ┌─────────┐   ┌─────────┐           │     │  │
│  │  │  │  Lint   │──▶│  Build  │──▶│  Test   │ ✅        │     │  │
│  │  │  └─────────┘   └─────────┘   └─────────┘           │     │  │
│  │  └─────────────────────────────────────────────────────┘     │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

Legend: ✅ 구현됨 | 📋 계획됨
```

### 2.2 도구 생태계

```
CI/CD Pipeline
├── GitHub Actions              # CI 플랫폼
│   └── ubuntu-latest + Node 20 # 실행 환경
├── Quality Gates
│   ├── ESLint 9.x              # 코드 린팅
│   ├── Prettier 3.x            # 코드 포맷팅
│   └── TypeScript 5.x          # 타입 검사
├── Build Tools
│   ├── Vite 6.x                # 빌드 번들러
│   └── tsc                     # TypeScript 컴파일
├── Git Hooks
│   ├── Husky 9.x               # Git 훅 관리
│   └── lint-staged 16.x        # 스테이지 파일 린트
└── Testing (Local Only → CI Integration 📋)
    ├── Vitest 3.x              # 테스트 프레임워크
    └── v8 coverage             # 커버리지 리포터
```

### 2.3 브랜치 전략

```
master ─────────────────────────────────────▶ Production
   │
   └──▶ develop ────────────────────────────▶ Development
           │
           ├──▶ feature/* ──────────────────▶ Feature branches
           ├──▶ fix/* ──────────────────────▶ Bug fix branches
           └──▶ docs/* ─────────────────────▶ Documentation branches
```

### 2.4 장애 대응 (Failure Handling)

| 장애 상황                  | 영향             | 대응 방안                                      |
| -------------------------- | ---------------- | ---------------------------------------------- |
| GitHub Actions Runner 불가 | CI 일시 차단     | GitHub 자동 재시도 (3회), 상태 페이지 모니터링 |
| npm registry 다운          | 의존성 설치 실패 | npm 캐시 사용, 대기 후 재시도                  |
| 테스트 실패                | PR 머지 차단     | PR status check로 머지 방지, 실패 로그 확인    |
| 빌드 타임아웃              | Job 강제 종료    | 15분 타임아웃 설정, 캐시 최적화 검토           |
| 동시 실행 충돌             | 리소스 낭비      | concurrency group으로 이전 실행 취소           |

**알림 체계:**

- PR 작성자에게 GitHub 알림 자동 전송
- Status check로 PR 페이지에서 실패 원인 확인 가능

---

## 3. Implementation Checklist (구현 체크리스트)

### 3.1 Phase 1.1.1: 기본 CI/CD (✅ 완료)

- ✅ GitHub Actions 워크플로우 설정 (`.github/workflows/ci.yml`)
- ✅ Pre-commit 훅 설정 (Husky + lint-staged)
- ✅ Pre-push 훅 설정 (lint + type-check + build)
- ✅ ESLint 설정 (TypeScript strict + React Hooks)
- ✅ Prettier 설정 (Tailwind CSS 플러그인 포함)
- ✅ TypeScript strict mode 활성화
- ✅ Vite 빌드 최적화 (코드 스플리팅)

### 3.2 Phase 1.1.2: 테스트 CI 통합 (✅ 완료)

- ✅ GitHub Actions에 `npm run test:coverage` 스텝 추가
- ✅ Pre-push 훅에 테스트 실행 추가
- ✅ CI에서 커버리지 리포트 생성 (artifact 업로드)
- ✅ 커버리지 임계값 검증 (MVP: 20%/50%, 목표: 70%/60%)
- ✅ CI에 `npm audit --audit-level=high` 추가 (보안 기준선)
- ✅ CI에 concurrency, timeout-minutes 추가
- [ ] PR에 커버리지 코멘트 자동 추가 (선택 - 스킵)

### 3.3 Phase 1.1.3: 확장 DevOps (📋 Extension)

- [ ] 배포 자동화 워크플로우 (별도 Phase)
- [ ] 보안 취약점 스캔 (별도 Phase)
- [ ] 성능 벤치마킹 (별도 Phase)
- [ ] 환경별 설정 관리 (별도 Phase)

---

## 4. Key Implementation Details (핵심 구현 상세)

### 4.1 GitHub Actions 워크플로우

**파일**: `.github/workflows/ci.yml`

```yaml
name: CI

on:
    push:
        branches: [master, develop]
    pull_request:
        branches: [master, develop]

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    quality:
        name: Code Quality
        runs-on: ubuntu-latest
        timeout-minutes: 15

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
                  cache: 'npm'

            - name: Install dependencies
              run: npm ci

            - name: Security audit
              run: npm audit --audit-level=high
              continue-on-error: true

            - name: Type check
              run: npm run type-check

            - name: Lint
              run: npm run lint

            - name: Test with coverage
              run: npm run test:coverage

            - name: Build
              run: npm run build

            - name: Upload coverage report
              uses: actions/upload-artifact@v4
              if: always()
              with:
                  name: coverage-report
                  path: coverage/
                  retention-days: 7
```

### 4.2 Git Hooks 설정

**Pre-commit** (`.husky/pre-commit`):

```bash
npx lint-staged
```

**Pre-push** (`.husky/pre-push`):

```bash
npm run lint && npm run type-check && npm run test && npm run build
```

**lint-staged 설정** (`package.json`):

```json
{
    "lint-staged": {
        "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
        "*.{json,md,css}": ["prettier --write"]
    }
}
```

### 4.3 ESLint 핵심 규칙

**파일**: `eslint.config.js`

| 규칙 카테고리   | 설정 내용                                                       |
| --------------- | --------------------------------------------------------------- |
| **TypeScript**  | strict rules 활성화                                             |
| **React Hooks** | recommended-latest                                              |
| **Import 순서** | builtin → external → internal → parent → sibling → index → type |
| **네이밍**      | camelCase (변수), PascalCase (타입/컴포넌트), UPPER_CASE (상수) |
| **Type Import** | 타입 전용 import 강제                                           |

### 4.4 Prettier 설정

**파일**: `.prettierrc`

```json
{
    "semi": true,
    "singleQuote": true,
    "tabWidth": 4,
    "trailingComma": "es5",
    "plugins": ["prettier-plugin-tailwindcss"]
}
```

### 4.5 TypeScript Strict 설정

**파일**: `tsconfig.app.json`

| 옵션                         | 값   | 설명                          |
| ---------------------------- | ---- | ----------------------------- |
| `strict`                     | true | 종합 strict mode              |
| `noUncheckedIndexedAccess`   | true | 인덱스 접근 시 undefined 체크 |
| `exactOptionalPropertyTypes` | true | optional 속성 타입 정확성     |
| `useUnknownInCatchVariables` | true | catch 변수 unknown 타입       |
| `noFallthroughCasesInSwitch` | true | switch fallthrough 방지       |

### 4.6 Vitest 설정 (로컬)

**파일**: `vitest.config.ts`

| 설정                | 값                                                     | 설명            |
| ------------------- | ------------------------------------------------------ | --------------- |
| environment         | jsdom                                                  | DOM 테스트 환경 |
| globals             | true                                                   | 전역 테스트 API |
| coverage.provider   | v8                                                     | 커버리지 엔진   |
| coverage.thresholds | MVP: 20% line, 50% branch (목표: 70% line, 60% branch) | 점진적 향상     |

---

## 5. Testing Strategy (테스트 전략)

> **테스트 범위**: MVP는 단위 테스트(Vitest)만 포함합니다. E2E 테스트(Playwright)는 Phase 5.x에서 구현 예정입니다.

### 5.1 현재 상태 (Phase 1.1.2 완료)

| 테스트 유형                            | 로컬 | CI  | 상태      |
| -------------------------------------- | ---- | --- | --------- |
| 타입 체크                              | ✅   | ✅  | 구현됨    |
| 린트                                   | ✅   | ✅  | 구현됨    |
| 빌드 검증                              | ✅   | ✅  | 구현됨    |
| 단위 테스트                            | ✅   | ✅  | ✅ 구현됨 |
| 커버리지 (MVP: 20%/50%, 목표: 70%/60%) | ✅   | ✅  | ✅ 구현됨 |
| 보안 스캔 (npm audit)                  | ✅   | ✅  | ✅ 구현됨 |

### 5.2 CI 검증 체크리스트

| 검증 항목   | 명령어                         | CI 단계 |
| ----------- | ------------------------------ | ------- |
| 보안 스캔   | `npm audit --audit-level=high` | ✅      |
| 타입 체크   | `npm run type-check`           | ✅      |
| 린트        | `npm run lint`                 | ✅      |
| 단위 테스트 | `npm run test:coverage`        | ✅      |
| 빌드        | `npm run build`                | ✅      |

### 5.3 Quality Gates

```
PR Merge Requirements:
├── ✅ Security: npm audit --audit-level=high
├── ✅ Type Check: Must pass
├── ✅ Lint: Must pass
├── ✅ Tests: Must pass with coverage
├── ✅ Coverage: MVP ≥20% line, ≥50% branch (목표: 70%/60%)
└── ✅ Build: Must succeed
```

---

## 6. Routes & Navigation (라우트 및 네비게이션)

> **N/A**: CI/CD는 인프라 구성 요소로 라우트가 없습니다.

---

## 7. Dependencies & References (의존성 및 참조)

### 7.1 핵심 패키지

| 패키지        | 버전    | 용도               |
| ------------- | ------- | ------------------ |
| `husky`       | ^9.1.7  | Git hooks 관리     |
| `lint-staged` | ^16.2.7 | 스테이지 파일 린트 |
| `eslint`      | ^9.39.1 | 코드 린팅          |
| `prettier`    | ^3.6.2  | 코드 포맷팅        |
| `typescript`  | ^5.6.3  | 타입 체크          |
| `vitest`      | ^3.2.4  | 테스트 프레임워크  |
| `vite`        | ^6.0.11 | 빌드 도구          |

### 7.2 참조 문서

| 문서                                     | 역할            |
| ---------------------------------------- | --------------- |
| [DEV_GUIDE.md](../../DEV_GUIDE.md)       | 개발 컨벤션     |
| [ARCHITECTURE.md](../../ARCHITECTURE.md) | 시스템 아키텍처 |
| [ROADMAP.md](../../ROADMAP.md)           | 프로젝트 로드맵 |

### 7.3 외부 참조

| 리소스              | URL                                |
| ------------------- | ---------------------------------- |
| GitHub Actions Docs | https://docs.github.com/en/actions |
| Husky Docs          | https://typicode.github.io/husky   |
| ESLint Docs         | https://eslint.org/docs/latest     |
| Vitest Docs         | https://vitest.dev/guide           |

### 7.4 관련 Phase

| Phase     | 관계 | 설명                                  |
| --------- | ---- | ------------------------------------- |
| Phase 1.2 | 후속 | Teapot Demo - CI/CD 위에 첫 기능 구현 |
| Phase 2.x | 의존 | CAD Viewer - CI/CD로 품질 보장        |
| Phase 5.x | 연관 | Performance - CI에 벤치마크 통합 가능 |

---

## 8. Post-Completion Checklist (완료 후 체크리스트)

### 8.1 연관 문서 업데이트 (Claude 자동 제안)

| 문서                             | 업데이트 내용                            |
| -------------------------------- | ---------------------------------------- |
| `docs/phases/PHASE_NAV_GUIDE.md` | Phase 1.1 문서 링크 추가, 상태 업데이트  |
| `docs/ROADMAP.md`                | Phase 1.1 진행률 (100%) 반영             |
| `docs/ARCHITECTURE.md`           | DevOps 섹션에 CI/CD 파이프라인 참조 추가 |

### 8.2 Phase 1.1.2 완료 시 추가 업데이트 (✅ 완료)

- ✅ `.github/workflows/ci.yml`에 test, audit, concurrency, timeout 추가
- ✅ `.husky/pre-push`에 test 명령어 추가
- ✅ Phase 진행률 100%로 업데이트
- [ ] ROADMAP.md 상태 ✅ 완료로 변경

---

## 9. Changelog (변경 이력)

| 버전  | 날짜       | 변경 내용                                                                                                                                                                                                                                |
| ----- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.0.1 | 2025-12-17 | 체크리스트 표기 통일 ([x]→✅), 실제 파일과 예시 코드 동기화 (ci.yml, pre-push), 다이어그램 상태 업데이트                                                                                                                                 |
| 0.0.0 | 2025-12-16 | 초기 문서 작성 (Phase 1.1.1 완료, 1.1.2 계획); Expert Panel 리뷰 반영 (성공 기준, 장애 대응, 커버리지 명세); Phase 1.1.2 구현 완료 (ci.yml 테스트/보안/concurrency, pre-push 테스트); 커버리지 임계값 현실화 (MVP 20%/50%, 목표 70%/60%) |
