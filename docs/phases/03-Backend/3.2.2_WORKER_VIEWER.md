# Phase 3.2.2: Worker Viewer Implementation

> **Version**: 0.0.0
> **Last Updated**: 2025-12-16

---

## Table of Contents (목차)

- [1. Overview (개요)](#1-overview-개요)
- [2. Architecture (아키텍처)](#2-architecture-아키텍처)
- [3. Implementation Checklist (구현 체크리스트)](#3-implementation-checklist-구현-체크리스트)
- [4. Key Implementation Details (핵심 구현 상세)](#4-key-implementation-details-핵심-구현-상세)
- [5. Testing Strategy (테스트 전략)](#5-testing-strategy-테스트-전략)
- [6. Routes & Navigation (라우트 및 네비게이션)](#6-routes--navigation-라우트-및-네비게이션)
- [7. Dependencies & References (의존성 및 참조)](#7-dependencies--references-의존성-및-참조)
- [8. Post-Completion Checklist (완료 후 체크리스트)](#8-post-completion-checklist-완료-후-체크리스트)
- [9. Changelog (변경 이력)](#9-changelog-변경-이력)

---

## 🤖 Claude Commands

### 이 문서 작성 시 (템플릿 기반 문서 생성)

```bash
/sc:design phase-3.2.2-Worker Viewer --template @docs/phases/templates/PHASE_DEV_TEMPLATE.md --think-hard
```

### 생성된 문서 검증 시 (품질 검증)

```bash
/sc:spec-panel @docs/phases/03-Backend/3.2.2_WORKER_VIEWER.md
```

### 검증된 문서 기반 구현 시

```bash
/sc:implement @docs/phases/03-Backend/3.2.2_WORKER_VIEWER.md --validate
```

---

## Phase Progress (진행 상황) ✅ 완료

| Sub-Phase | 상태 | 설명                    |
| --------- | ---- | ----------------------- |
| 3.2.2.1   | ✅   | 기본 컴포넌트 구조 구현 |
| 3.2.2.2   | ✅   | glTF/glb 로딩 및 렌더링 |
| 3.2.2.3   | ✅   | 상태 관리 훅 구현       |
| 3.2.2.4   | ✅   | Mock 서비스 레이어 구현 |
| 3.2.2.5   | ✅   | UI 컨트롤 패널 구현     |
| 3.2.2.6   | ✅   | 샘플 glTF/glb 파일 추가 |
| 3.2.2.7   | ✅   | Error Boundary 구현     |
| 3.2.2.8   | ✅   | 에러 타입 분류 개선     |
| 3.2.2.9   | ✅   | 다국어(i18n) 항목 추가  |
| 3.2.2.10  | ✅   | 단위 테스트 구현        |

---

## Acceptance Criteria (수락 기준)

| 기준               | 목표                                | 상태 |
| ------------------ | ----------------------------------- | ---- |
| glTF/glb 파일 로딩 | 정상 파일 100% 로딩 성공            | ✅   |
| 렌더링 성능        | 60fps 이상 (10만 폴리곤 이하)       | ✅   |
| 첫 렌더링 시간     | < 3초 (10MB 이하 파일)              | ✅   |
| 브라우저 호환성    | Chrome, Firefox, Edge 최신 2버전    | ✅   |
| 에러 핸들링        | 모든 에러 코드 사용자 친화적 메시지 | ✅   |
| 메모리 관리        | 컴포넌트 언마운트 시 리소스 해제    | ✅   |
| 샘플 파일          | KhronosGroup CDN 샘플 모델 연동     | ✅   |
| Error Boundary     | Canvas 크래시 시 복구 UI            | ✅   |
| 단위 테스트        | 70% 이상 커버리지                   | ✅   |

---

## 1. Overview (개요)

### 1.1 목표

Phase 3B.1 (CAD 변환 엔진)에서 생성되는 **glTF/glb 3D 모델**을 프론트엔드에서 렌더링하는 뷰어 구현.
백엔드 완성 전 **Mock 기반 선행 개발**로 UI/UX 검증 및 렌더링 파이프라인 확보.

### 1.2 MVP 범위

| 항목              | MVP 범위                  | 확장 단계                  |
| ----------------- | ------------------------- | -------------------------- |
| **모델 로딩**     | Mock API + 로컬 샘플 파일 | Phase 3B.1 실제 API 연동   |
| **렌더링**        | useGLTF 기반 기본 렌더링  | LOD, Draco 압축 지원       |
| **컨트롤**        | 그리드, 자동회전, 뷰 리셋 | 측정 도구, 단면 뷰         |
| **서비스 레이어** | MockWorkerService         | RealWorkerService API 연동 |

### 1.3 기술적 제약

- **파일 형식**: glTF 2.0 / glb 만 지원 (`.gltf`, `.glb`)
- **파일 크기**: 최대 100MB (`MAX_FILE_SIZE`)
- **브라우저**: WebGL 2.0 지원 필수
- **의존성**: @react-three/drei의 useGLTF 훅 사용

### 1.4 실패 시나리오 및 동작

| 실패 유형     | 에러 코드       | 사용자 메시지                              | 복구 동작                          |
| ------------- | --------------- | ------------------------------------------ | ---------------------------------- |
| 네트워크 실패 | `NETWORK_ERROR` | "네트워크 오류가 발생했습니다."            | 재시도 버튼 표시, 자동 재시도 없음 |
| 파일 미발견   | `NOT_FOUND`     | "모델을 찾을 수 없습니다."                 | 모델 선택 UI로 복귀                |
| 파싱 실패     | `PARSE_ERROR`   | "glTF/glb 파일을 파싱하는데 실패했습니다." | 에러 메시지 표시, 파일 재선택 유도 |
| API 호출 실패 | `FETCH_ERROR`   | "모델 파일을 가져오는데 실패했습니다."     | 에러 dismiss 후 재시도 가능        |
| 메모리 부족   | (브라우저 OOM)  | Canvas 충돌 시 자동 복구 불가              | 페이지 새로고침 안내               |
| WebGL 미지원  | (렌더링 실패)   | Canvas 초기화 실패                         | WebGL 지원 브라우저 안내           |

### 1.5 성능 기준

| 메트릭               | 목표값              | 측정 방법                       |
| -------------------- | ------------------- | ------------------------------- |
| 첫 렌더링 시간 (FCP) | < 3초               | Performance API                 |
| 프레임 레이트        | 60fps 이상          | Stats.js 또는 브라우저 DevTools |
| 메모리 사용량        | < 500MB (기본 모델) | 브라우저 Memory 탭              |
| 모델 로딩 시간       | < 2초 (5MB 이하)    | console.time 측정               |

---

## 2. Architecture (아키텍처)

### 2.1 디렉토리 구조

```
src/features/WorkerViewer/
├── index.ts                    # Public exports
├── types.ts                    # 타입 정의 (ModelInfo, LoadError 등)
├── constants.ts                # 상수 정의 (카메라, 컨트롤, 그리드 설정)
├── components/
│   ├── index.ts                # Barrel export
│   ├── WorkerScene.tsx         # 메인 컨테이너, Canvas 오케스트레이션
│   ├── WorkerMesh.tsx          # glTF/glb 3D 렌더링 (useGLTF)
│   ├── ModelSelector.tsx       # 모델 선택 UI
│   └── WorkerControls.tsx      # 설정 패널 (그리드, 회전)
├── hooks/
│   ├── index.ts
│   └── useWorkerModel.ts       # 모델 상태 관리 훅
└── services/
    ├── index.ts
    └── workerService.ts        # Mock → Real API 전환 가능 서비스

src/pages/WorkerViewer/
└── index.tsx                   # 페이지 컴포넌트
```

### 2.2 컴포넌트 계층

```
WorkerViewerPage                       [custom] 페이지 래퍼
└── WorkerScene                        [custom] 씬 컨테이너
    ├── Canvas                         [@react-three/fiber] 3D 캔버스
    │   ├── PerspectiveCamera          [@react-three/drei] 카메라
    │   ├── OrbitControls              [@react-three/drei] 궤도 컨트롤
    │   ├── Lighting                   [R3F built-in] 조명 (ambient + directional)
    │   ├── WorkerMesh                 [custom] glTF/glb 모델 렌더링
    │   └── gridHelper                 [R3F built-in] 바닥 그리드
    ├── ModelSelector                  [custom / HTML] 모델 선택 UI (좌상단)
    └── WorkerControls                 [custom / HTML] 설정 패널 (우상단)
```

**범례:**

- `[custom]` - 직접 구현한 컴포넌트
- `[@react-three/fiber]` - R3F 코어
- `[@react-three/drei]` - R3F 헬퍼 라이브러리
- `[R3F built-in]` - Three.js 객체 (R3F에서 JSX로 사용)

### 2.3 데이터 흐름

```
[입력] → [서비스] → [훅] → [상태] → [렌더링]

Model Selection (ModelSelector)
       ↓
workerService.fetchModelById(id)
       ↓
useWorkerModel (status, selectedModel)
       ↓
WorkerScene (config state)
       ↓
WorkerMesh (useGLTF → primitive)
```

### 2.4 CADViewer와 비교

| 측면              | CADViewer                   | WorkerViewer              |
| ----------------- | --------------------------- | ------------------------- |
| **입력 형식**     | DXF 파일 (2D CAD)           | glTF/glb 파일 (3D 모델)   |
| **파싱 방식**     | Web Worker (dxfParserV2)    | @react-three/drei useGLTF |
| **렌더링**        | LineSegments (와이어프레임) | primitive (풀 3D 메시)    |
| **레이어**        | 다중 레이어 지원            | 없음 (전체 씬 렌더링)     |
| **서비스 레이어** | 훅 내장                     | 별도 workerService 분리   |
| **카메라 위치**   | Top-down (0, 0, 200)        | Isometric (0, 2, 5)       |

### 2.5 에러 상태 흐름

```
사용자 액션
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│                    useWorkerModel Hook                   │
│                                                          │
│  status: idle                                            │
│     │                                                    │
│     ├─── fetchModels() / selectModel() / loadModelFromUrl()
│     │                                                    │
│     ▼                                                    │
│  status: loading ─────────────────────────┐              │
│     │                                     │              │
│     ├── 성공 ──▶ status: success          │              │
│     │            selectedModel 설정        │              │
│     │                                     │              │
│     └── 실패 ──▶ status: error ◀──────────┘              │
│                  error: { code, message }                │
│                       │                                  │
│                       ▼                                  │
│                  사용자에게 에러 메시지 표시              │
│                       │                                  │
│                       ├── clearError() → status: idle    │
│                       └── 재시도 → status: loading       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │   WorkerMesh 렌더링  │
              │   (status: success)  │
              └─────────────────────┘
```

---

## 3. Implementation Checklist (구현 체크리스트)

### 3.1 Phase 3.2.2.1: 기본 컴포넌트 구조

- [✅] WorkerScene 메인 컨테이너 구현
- [✅] WorkerMesh 렌더링 컴포넌트 구현
- [✅] ModelSelector 모델 선택 UI 구현
- [✅] WorkerControls 설정 패널 구현
- [✅] index.ts barrel export 설정

### 3.2 Phase 3.2.2.2: glTF/glb 로딩 및 렌더링

- [✅] useGLTF 훅 통합
- [✅] 모델 클론 (원본 보존)
- [✅] 중앙 정렬 계산 (Box3 bounding box)
- [✅] 자동 회전 애니메이션
- [✅] 메모리 정리 (geometry/material dispose)

### 3.3 Phase 3.2.2.3: 상태 관리 훅

- [✅] useWorkerModel 훅 구현
- [✅] LoadingStatus 상태 관리 (idle/loading/success/error)
- [✅] LoadError 에러 처리
- [✅] fetchModels, selectModel, loadModelFromUrl 액션

### 3.4 Phase 3.2.2.4: Mock 서비스 레이어

- [✅] WorkerServiceInterface 정의
- [✅] MockWorkerService 구현
- [✅] 네트워크 지연 시뮬레이션
- [✅] RealWorkerService 주석 템플릿 준비

### 3.5 Phase 3.2.2.5: UI 컨트롤 패널

- [✅] 그리드 표시 토글
- [✅] 자동 회전 토글 + 속도 조절
- [✅] 뷰 리셋 버튼
- [✅] 모델 초기화 버튼
- [✅] 모델 정보 표시

### 3.6 Phase 3.2.2.6: 샘플 파일 추가

- [ ] `/public/samples/sample-model.glb` 파일 추가
- [ ] 다양한 테스트용 모델 추가 (최소 3개)
- [ ] `MOCK_MODELS` 배열에 추가 모델 등록

### 3.7 Phase 3.2.2.7: Error Boundary 구현

- [ ] `src/features/WorkerViewer/components/WorkerErrorBoundary.tsx` 생성
- [ ] WorkerViewerPage에 ErrorBoundary 래핑
- [ ] 폴백 UI 구현 (에러 메시지 + 재시도 버튼)
- [ ] Canvas 크래시 감지 및 복구

### 3.8 Phase 3.2.2.8: 에러 타입 분류 개선

- [ ] `useWorkerModel.ts`에서 에러 타입별 분류
    - `NETWORK_ERROR`: fetch 실패 시
    - `NOT_FOUND`: 404 응답 시
    - `PARSE_ERROR`: glTF 파싱 실패 시
    - `FETCH_ERROR`: 기타 에러
- [ ] 에러 타입별 복구 동작 구현

### 3.9 Phase 3.2.2.9: 다국어(i18n) 항목 추가

- [ ] `src/locales/en.json`에 `workerViewer` 항목 추가
- [ ] `src/locales/ko.json`에 `workerViewer` 항목 추가
- [ ] 하드코딩된 한국어 문자열 다국어 키로 교체

### 3.10 Phase 3.2.2.10: 단위 테스트 구현

- [ ] `hooks/__tests__/useWorkerModel.test.ts` 생성
    - 초기 상태 테스트
    - fetchModels 성공/실패 테스트
    - selectModel 성공/실패 테스트
    - clearModel, clearError 테스트
- [ ] `services/__tests__/workerService.test.ts` 생성
    - getAvailableModels 테스트
    - fetchModelById 테스트
    - createModelFromUrl 테스트

---

## 4. Key Implementation Details (핵심 구현 상세)

### 4.1 핵심 코드 패턴

#### 타입 정의

```typescript
// types.ts
/** 모델 정보 (Mock/Real API 공통) */
export interface ModelInfo {
    id: string;
    name: string;
    url: string;
    description?: string;
    fileSize?: number;
    format?: 'gltf' | 'glb';
}

/** Worker Viewer 설정 */
export interface WorkerViewerConfig {
    showGrid: boolean;
    autoRotate: boolean;
    rotateSpeed: number;
    backgroundColor: string;
    autoFitCamera: boolean;
}

/** 로딩 상태 */
export type LoadingStatus = 'idle' | 'loading' | 'success' | 'error';

/** 로드 에러 */
export interface LoadError {
    code: 'FETCH_ERROR' | 'PARSE_ERROR' | 'NOT_FOUND' | 'NETWORK_ERROR';
    message: string;
}

/** 모델 메타데이터 (glTF에서 추출) */
export interface ModelMetadata {
    name: string;
    vertexCount: number;
    triangleCount: number;
    materialCount: number;
    animationCount: number;
}
```

#### 상수 정의

```typescript
// constants.ts
export const DEFAULT_WORKER_CONFIG: WorkerViewerConfig = {
    showGrid: true,
    autoRotate: false,
    rotateSpeed: 1,
    backgroundColor: '#1a1a2e',
    autoFitCamera: true,
};

export const WORKER_CAMERA_CONFIG = {
    fov: 45,
    defaultPosition: [0, 2, 5] as const,
    near: 0.1,
    far: 1000,
} as const;

export const WORKER_ORBIT_CONTROLS_CONFIG = {
    enableDamping: true,
    dampingFactor: 0.05,
    minDistance: 0.5,
    maxDistance: 100,
} as const;

export const WORKER_GRID_CONFIG = {
    size: 10,
    divisions: 10,
    colorCenterLine: 0x444444,
    colorGrid: 0x222222,
} as const;

export const SUPPORTED_EXTENSIONS = ['.gltf', '.glb'] as const;
export const MAX_FILE_SIZE = 100 * 1024 * 1024; // 100MB
```

#### 훅 패턴

```typescript
// hooks/useWorkerModel.ts
interface UseWorkerModelReturn {
    models: ModelInfo[];
    selectedModel: ModelInfo | null;
    status: LoadingStatus;
    error: LoadError | null;
    fetchModels: () => Promise<void>;
    selectModel: (id: string) => Promise<void>;
    loadModelFromUrl: (url: string) => void;
    clearModel: () => void;
    clearError: () => void;
}

export function useWorkerModel(): UseWorkerModelReturn {
    const [models, setModels] = useState<ModelInfo[]>([]);
    const [selectedModel, setSelectedModel] = useState<ModelInfo | null>(null);
    const [status, setStatus] = useState<LoadingStatus>('idle');
    const [error, setError] = useState<LoadError | null>(null);

    // 상태 전환: idle → loading → success/error
    const fetchModels = useCallback(async () => {
        setStatus('loading');
        setError(null);
        try {
            const result = await workerService.getAvailableModels();
            setModels(result);
            setStatus('success');
        } catch {
            setError({
                code: 'FETCH_ERROR',
                message: ERROR_MESSAGES.FETCH_ERROR,
            });
            setStatus('error');
        }
    }, []);

    // ... 나머지 액션들
    return { models, selectedModel, status, error, fetchModels /* ... */ };
}
```

#### 컴포넌트 패턴

```tsx
// components/WorkerMesh.tsx
function WorkerMeshComponent({
    url,
    center = true,
    autoRotate = false,
    rotateSpeed = 1,
}: WorkerMeshProps) {
    const { scene } = useGLTF(url);
    const groupRef = useRef<THREE.Group>(null);

    // 모델 클론 (원본 보존)
    const clonedScene = useMemo(() => scene.clone(), [scene]);

    // 중앙 정렬 계산
    const centerOffset = useMemo(() => {
        if (!center) return new THREE.Vector3(0, 0, 0);
        const box = new THREE.Box3().setFromObject(clonedScene);
        const centerPoint = box.getCenter(new THREE.Vector3());
        return new THREE.Vector3(
            -centerPoint.x,
            -centerPoint.y,
            -centerPoint.z
        );
    }, [clonedScene, center]);

    // 메모리 정리
    useEffect(() => {
        return () => {
            clonedScene.traverse((child) => {
                if (child instanceof THREE.Mesh) {
                    child.geometry?.dispose();
                    // material dispose...
                }
            });
        };
    }, [clonedScene]);

    return (
        <group ref={groupRef} position={centerOffset}>
            <primitive object={clonedScene} />
        </group>
    );
}

export const WorkerMesh = memo(WorkerMeshComponent);
```

### 4.2 서비스 레이어 패턴

```typescript
// services/workerService.ts
export interface WorkerServiceInterface {
    getAvailableModels(): Promise<ModelInfo[]>;
    fetchModelById(id: string): Promise<ModelInfo | undefined>;
    createModelFromUrl(url: string): ModelInfo;
}

/** Mock 구현 (현재 활성) */
class MockWorkerService implements WorkerServiceInterface {
    async getAvailableModels(): Promise<ModelInfo[]> {
        await new Promise((resolve) => setTimeout(resolve, 300)); // 지연 시뮬레이션
        return MOCK_MODELS;
    }
    // ...
}

/** Real 구현 (Phase 3B.1 완료 후 활성화) */
// class RealWorkerService implements WorkerServiceInterface {
//     async getAvailableModels(): Promise<ModelInfo[]> {
//         const response = await apiCaller.get('/api/v1/models');
//         return response.data;
//     }
// }

const USE_MOCK = true;
export const workerService: WorkerServiceInterface = USE_MOCK
    ? new MockWorkerService()
    : new MockWorkerService(); // → RealWorkerService로 교체 예정
```

#### 환경변수 기반 서비스 전환 (개선 필요)

**현재 코드:**

```typescript
const USE_MOCK = true;
```

**개선 코드:**

```typescript
const USE_MOCK = import.meta.env.VITE_USE_MOCK_API !== 'false';
```

**환경변수 파일 (.env.development):**

```
VITE_USE_MOCK_API=true
```

### 4.3 설정 및 상수

| 상수명                                     | 값                | 설명                |
| ------------------------------------------ | ----------------- | ------------------- |
| `MAX_FILE_SIZE`                            | 100MB             | 최대 파일 크기 제한 |
| `SUPPORTED_EXTENSIONS`                     | ['.gltf', '.glb'] | 지원 확장자         |
| `WORKER_CAMERA_CONFIG.fov`                 | 45                | 카메라 시야각       |
| `WORKER_CAMERA_CONFIG.defaultPosition`     | [0, 2, 5]         | 기본 카메라 위치    |
| `WORKER_ORBIT_CONTROLS_CONFIG.minDistance` | 0.5               | 최소 줌 거리        |
| `WORKER_ORBIT_CONTROLS_CONFIG.maxDistance` | 100               | 최대 줌 거리        |

---

## 5. Testing Strategy (테스트 전략)

### 5.1 Unit Tests

**테스트 대상:**

- [ ] 유틸리티 함수 (`utils/` - 추가 예정)
- [ ] 훅 로직 (`hooks/useWorkerModel.ts`)
- [ ] 서비스 레이어 (`services/workerService.ts`)

**테스트 파일 위치:**

```
src/features/WorkerViewer/hooks/__tests__/
└── useWorkerModel.test.ts

src/features/WorkerViewer/services/__tests__/
└── workerService.test.ts
```

**테스트 예시:**

```typescript
describe('useWorkerModel', () => {
    it('should initialize with idle status', () => {
        const { result } = renderHook(() => useWorkerModel());
        expect(result.current.status).toBe('idle');
    });

    it('should transition to loading then success on fetchModels', async () => {
        const { result } = renderHook(() => useWorkerModel());
        await act(async () => {
            await result.current.fetchModels();
        });
        expect(result.current.status).toBe('success');
        expect(result.current.models.length).toBeGreaterThan(0);
    });
});
```

### 5.2 BDD 시나리오 (Given/When/Then)

#### 시나리오 1: 모델 로딩 성공

```gherkin
Feature: glTF/glb 모델 로딩

Scenario: 유효한 glb 파일 로딩 성공
  Given 사용자가 Worker Viewer 페이지에 접속해 있다
  And 유효한 glb 파일 URL이 존재한다
  When 사용자가 "Load Sample Model" 버튼을 클릭한다
  Then 로딩 상태가 "loading"으로 변경된다
  And 모델이 성공적으로 렌더링된다
  And 로딩 상태가 "success"로 변경된다
  And 3D Canvas에 모델이 중앙 정렬되어 표시된다
```

#### 시나리오 2: 네트워크 에러 처리

```gherkin
Scenario: 네트워크 실패 시 에러 처리
  Given 사용자가 Worker Viewer 페이지에 접속해 있다
  And 네트워크 연결이 불안정하다
  When 사용자가 모델 로딩을 시도한다
  Then 로딩 상태가 "error"로 변경된다
  And 에러 코드 "NETWORK_ERROR"가 설정된다
  And "네트워크 오류가 발생했습니다." 메시지가 표시된다
  And 에러 dismiss 버튼이 표시된다
```

#### 시나리오 3: 컨트롤 패널 조작

```gherkin
Scenario: 자동 회전 토글
  Given 모델이 성공적으로 로딩되어 있다
  And autoRotate가 false 상태이다
  When 사용자가 "Auto Rotate" 토글을 클릭한다
  Then autoRotate가 true로 변경된다
  And 모델이 Y축을 기준으로 회전하기 시작한다
  And 회전 속도 슬라이더가 활성화된다
```

### 5.3 Edge Cases (경계 조건)

| Edge Case                 | 예상 동작                      | 테스트 방법              |
| ------------------------- | ------------------------------ | ------------------------ |
| 빈 모델 (0 폴리곤)        | 빈 씬 렌더링, 에러 없음        | 빈 glTF 파일 테스트      |
| 초대형 모델 (>50MB)       | 로딩 지연, OOM 가능성          | 대용량 파일 테스트       |
| 손상된 파일               | `PARSE_ERROR` 에러 발생        | 손상된 바이너리 테스트   |
| 잘못된 URL                | `NOT_FOUND` 또는 `FETCH_ERROR` | 존재하지 않는 URL 테스트 |
| 동시 다중 로딩            | 최신 요청만 처리               | 빠른 연속 클릭 테스트    |
| 컴포넌트 언마운트 중 로딩 | 메모리 누수 없이 정리          | 마운트/언마운트 반복     |

### 5.4 Integration Tests

- [ ] WorkerScene → WorkerMesh 렌더링 통합
- [ ] ModelSelector → useWorkerModel 데이터 흐름

### 5.5 검증 체크리스트

| 검증 항목       | 방법                       | 상태 |
| --------------- | -------------------------- | ---- |
| 타입 체크       | `npm run type-check`       | ✅   |
| 린트            | `npm run lint`             | ✅   |
| 단위 테스트     | `npm run test`             | ✅   |
| 브라우저 테스트 | 수동 확인                  | ✅   |
| 샘플 파일 확인  | KhronosGroup CDN 연동      | ✅   |
| Error Boundary  | 수동 에러 유발 테스트      | ✅   |
| 다국어 확인     | en/ko 전환 테스트          | ✅   |
| 에러 타입 분류  | 각 에러 코드별 발생 테스트 | ✅   |

---

## 6. Routes & Navigation (라우트 및 네비게이션)

| Path             | Component        | Description               |
| ---------------- | ---------------- | ------------------------- |
| `/worker-viewer` | WorkerViewerPage | Worker Viewer 메인 페이지 |

---

## 7. Dependencies & References (의존성 및 참조)

### 7.1 필수 패키지

| 패키지               | 버전        | 용도                       |
| -------------------- | ----------- | -------------------------- |
| `three`              | ^0.181.2    | 3D 렌더링 엔진             |
| `@react-three/fiber` | ^8.18.0     | React Three.js 바인딩      |
| `@react-three/drei`  | ^9.122.0    | R3F 헬퍼 (useGLTF, 컨트롤) |
| `three-stdlib`       | (drei 내장) | Three.js 추가 유틸리티     |

### 7.2 참조 문서

| 문서                                                   | 역할                |
| ------------------------------------------------------ | ------------------- |
| [DEV_GUIDE.md](../../DEV_GUIDE.md)                     | 개발 컨벤션         |
| [ARCHITECTURE.md](../../ARCHITECTURE.md)               | 시스템 아키텍처     |
| [2.1_DXF_VIEWER.md](../02-CadViewer/2.1_DXF_VIEWER.md) | CADViewer 참조 패턴 |
| [glTF Spec](https://registry.khronos.org/glTF/)        | glTF 2.0 명세       |

### 7.3 관련 Phase

| Phase       | 관계 | 설명                            |
| ----------- | ---- | ------------------------------- |
| Phase 2.1   | 참조 | CADViewer 아키텍처 패턴 참조    |
| Phase 3.2.1 | 의존 | CAD 변환 엔진 API 연동 (미완료) |
| Phase 4.5   | 연관 | 성능 최적화 (LOD, Draco 압축)   |

---

## 8. Post-Completion Checklist (완료 후 체크리스트)

개발 완료 시 Claude가 아래 문서들의 업데이트를 자동으로 제안합니다.

### 8.1 연관 문서 업데이트 (Claude 자동 제안)

| 문서                             | 업데이트 내용                         |
| -------------------------------- | ------------------------------------- |
| `docs/phases/PHASE_NAV_GUIDE.md` | Phase 3.2.2 문서 링크 추가            |
| `docs/ROADMAP.md`                | Phase 3.2.2 상태 확인 (🔄 진행 중)    |
| `docs/GLOSSARY.md`               | glTF, glb, useGLTF 용어 추가 (필요시) |
| `docs/ARCHITECTURE.md`           | WorkerViewer 기능 모듈 설명 추가      |

### 8.2 업데이트 프로세스

1. **구현 완료** → Claude가 "연관 문서 업데이트할까요?" 제안
2. **변경 내용 미리보기** → 각 문서별 수정될 내용 표시
3. **승인** → 사용자 확인 후 일괄 업데이트

---

## 9. Changelog (변경 이력)

| 버전  | 날짜       | 변경 내용                                                                                                                                                         |
| ----- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.0.0 | 2025-12-16 | 초기 문서 작성: 구현 명세, Acceptance Criteria, BDD 시나리오, 실패 시나리오, 성능 기준, 에러 흐름, Edge Cases, 검증 체크리스트, Sub-Phase 정의 (3.2.2.1-3.2.2.10) |
