# Phase 2.1: DXF CAD Viewer Implementation

> **Version**: 0.0.6
> **Last Updated**: 2025-12-29
> **Status**: 🔄 진행 중 (60%)

---

## Table of Contents (목차)

- [1. Overview (개요)](#1-overview-개요)
- [2. Architecture (아키텍처)](#2-architecture-아키텍처)
- [3. Implementation Checklist (구현 체크리스트)](#3-implementation-checklist-구현-체크리스트)
- [4. Key Implementation Details (핵심 구현 상세)](#4-key-implementation-details-핵심-구현-상세)
- [5. Testing Strategy (테스트 전략)](#5-testing-strategy-테스트-전략)
- [6. Routes & Navigation (라우트 및 네비게이션)](#6-routes--navigation-라우트-및-네비게이션)
- [7. Dependencies & References (의존성 및 참조)](#7-dependencies--references-의존성-및-참조)
- [8. Usage Guide (사용 가이드)](#8-usage-guide-사용-가이드)
- [9. Post-Completion Checklist (완료 후 체크리스트)](#9-post-completion-checklist-완료-후-체크리스트)
- [10. Changelog (변경 이력)](#10-changelog-변경-이력)

---

## 🤖 Claude Commands

### 이 문서 작성 시 (템플릿 기반 문서 생성)

```bash
/sc:design [phase-feature-name] --template @docs/phases/templates/PHASE_DEV_TEMPLATE.md [--think|--think-hard|--ultrathink]

# 예시
/sc:design phase-2a3-hatch-entity --template @docs/phases/templates/PHASE_DEV_TEMPLATE.md --think-hard
```

### 생성된 문서 검증 시 (품질 검증)

```bash
/sc:spec-panel @docs/phases/[Phase폴더]/[생성된파일].md

# 예시
/sc:spec-panel @docs/phases/02-CadViewer/2.1_DXF_VIEWER.md
```

### 검증된 문서 기반 구현 시

```bash
/sc:implement @docs/phases/[Phase폴더]/[THIS_FILE].md --validate

# 예시
/sc:implement @docs/phases/02-CadViewer/2.1_DXF_VIEWER.md --validate
```

### 개발 완료 후

구현이 완료되면 Claude가 자동으로 연관 문서 업데이트를 제안합니다:

- 어떤 내용이 변경되는지 미리 보여줌
- 승인 후 ROADMAP, ARCHITECTURE, README 업데이트

---

## Phase Progress (진행 상황)

| Sub-Phase | 상태 | 설명                                                  |
| --------- | ---- | ----------------------------------------------------- |
| 2.1.1     | ✅   | MVP - LINE 엔티티, 파일 업로드, 기본 렌더링           |
| 2.1.2     | ✅   | CIRCLE, ARC, POLYLINE 엔티티 확장                     |
| 2.1.3     | ✅   | HATCH (2D) 엔티티 + 렌더링 모드 토글                  |
| 2.1.4     | 📋   | 추가 엔티티 (TEXT, MTEXT, DIMENSION, SPLINE, ELLIPSE) |
| 2.1.5     | ✅   | 레이어별 색상/가시성 토글                             |
| 2.1.6     | ⏳   | HATCH 3D Extrusion (2D HATCH → ExtrudeGeometry)       |
| 2.1.7     | ⏳   | ShadingMode 적용 (3D 메쉬 Material 4가지 모드)        |
| 2.1.8     | 🔄   | 성능 최적화 (2D/3D Geometry 머징, WebWorker, LOD)     |
| 2.1.9     | 🔄   | Unit 테스트 (3D 포함 확장)                            |

---

## 1. Overview (개요)

### 1.1 목표

프론트엔드에서 DXF 파일을 파싱하여 3D 와이어프레임으로 렌더링 (백엔드 불필요)

### 1.2 MVP 범위

| 항목          | MVP 범위              | 확장 단계                                |
| ------------- | --------------------- | ---------------------------------------- |
| **파일 형식** | DXF (ASCII)           | DWG, PDF (Phase 2.2)                     |
| **엔티티**    | LINE                  | ARC, CIRCLE, POLYLINE ✅                 |
| **HATCH**     | -                     | 2D 면 채우기 ✅, 3D 돌출 ⏳              |
| **레이어**    | 단일 레이어           | 레이어별 색상/가시성 ✅                  |
| **UI**        | 파일 업로드 + 3D 뷰어 | 레이어 패널 ✅, 렌더링 토글 ✅           |
| **3D 렌더링** | -                     | HATCH 3D ⏳, ShadingMode (4가지 모드) ⏳ |
| **성능**      | 기본                  | 2D WebWorker/LOD ✅, 3D 최적화 🔄        |

### 1.3 기술적 제약

- DXF ASCII 형식만 지원 (바이너리 DXF 미지원)
- 파일 크기 제한: 20MB
- WebWorker 사용 조건: 파일 > 2MB

---

## 2. Architecture (아키텍처)

### 2.1 디렉토리 구조

```
src/features/CadViewer/
├── index.ts                    # Public exports
├── constants.ts                # 설정 상수
├── types/                      # 타입 정의 (구조화)
│   ├── index.ts                # 통합 export
│   ├── dxfEntity/              # DXF 엔티티 타입
│   │   ├── base.ts             # Point3D, BoundingBox 등
│   │   ├── library.ts          # dxf-parser 라이브러리 타입
│   │   └── parsed.ts           # 파싱된 엔티티 타입
│   └── dxfWorkerMsg/           # Worker 메시지 타입
│       └── index.ts
├── components/
│   ├── index.ts
│   ├── CadScene.tsx            # 메인 3D 씬 (TeapotScene 패턴)
│   ├── CadMesh.tsx             # 지오메트리 렌더러
│   └── LayerPanel.tsx          # 레이어 패널 UI
├── hooks/
│   ├── index.ts
│   ├── useDXFParser.ts         # DXF 파싱 훅
│   ├── useDXFWorker.ts         # WebWorker 파싱 훅
│   ├── useDxfLoader.ts         # DXF 파일 로딩 훅 (파일/URL 통합)
│   └── useCadMaterial.ts       # ⏳ ShadingMode Material 훅 (2.1.7)
├── services/                   # WebWorker 및 파싱 로직
│   ├── index.ts
│   ├── dxfParser.worker.ts     # DXF 파싱 WebWorker
│   ├── entityParsers.ts        # 엔티티별 파싱 로직
│   └── entityMath.ts           # 기하학 계산 유틸
├── utils/
│   ├── index.ts
│   ├── dxfToGeometry.ts        # DXF → Three.js 변환
│   ├── dxfSamples.ts           # 샘플 DXF 데이터
│   └── hatch3DExtrude.ts       # ⏳ HATCH 3D 돌출 (2.1.6)
└── __tests__/                  # 테스트 파일

src/components/                 # 공통 컴포넌트 (CadViewer에서 사용)
├── Common/DropZone/            # 드래그앤드롭 영역
├── FilePanel/                  # 파일 업로드 패널 (FileUploadBox, SampleList, UrlInput)
├── FilePanelViewer/            # 파일 패널 컴포지트 래퍼
└── SceneCanvasViewer/          # 3D 씬 컴포지트 래퍼

src/utils/                      # 중앙화된 유틸리티 (validators 이동됨)
├── fileValidator.ts            # 파일 유효성 검사
├── urlValidator.ts             # URL 유효성 검사
├── errorClassifier.ts          # 에러 분류
└── errorFormatter.ts           # 에러 메시지 포맷팅

src/pages/CadViewer/
└── index.tsx                   # 페이지 컴포넌트

public/samples/dxf/             # 테스트용 샘플 파일
├── simple-room.dxf
└── ...

tests/
├── mocks/dxf-parser/           # DXF 파서 목 (fixtures, config)
├── integration/                # 통합 테스트
└── scripts/                    # 테스트 스크립트
```

### 2.2 컴포넌트 계층

```
CadViewerPage                          [custom] 페이지 컴포넌트
├── FilePanelViewer                    [custom / HTML] 파일 업로드 컴포지트
│   ├── FileUploadBox                  [custom] 파일 드래그앤드롭 영역
│   ├── SampleList                     [custom] 서버 샘플 목록
│   └── UrlInput                       [custom] URL 입력
├── SceneCanvasViewer                  [custom] 3D 씬 컴포지트 래퍼
│   └── CadScene                       [custom] 씬 컨테이너
│       └── Canvas                     [@react-three/fiber] 3D 캔버스
│           ├── PerspectiveCamera      [@react-three/drei] 원근 카메라
│           ├── OrbitControls          [@react-three/drei] 궤도 컨트롤
│           ├── ambientLight           [R3F built-in] 환경광
│           ├── directionalLight       [R3F built-in] 방향광 (3D용)
│           ├── gridHelper             [R3F built-in] 바닥 그리드
│           └── CadMesh                [custom] CAD 지오메트리
│               ├── lineSegments       [R3F built-in] 2D 와이어프레임
│               ├── mesh (2D)          [R3F built-in] HATCH 2D 면 채우기
│               └── mesh (3D)          [R3F built-in] ⏳ HATCH 3D 돌출 + ShadingMode
├── ControlPanelViewer                 [custom / HTML] 공통 설정 패널
│   └── ShadingSelect                  [custom / HTML] ⏳ ShadingMode 선택 (2.1.7)
├── RenderModePanel                    [custom / HTML] 렌더링 모드 토글 (HATCH용)
├── DepthControl                       [custom / HTML] ⏳ 3D 돌출 깊이 조절 (2.1.6)
└── LayerPanel                         [custom / HTML] 레이어 토글
```

**범례:**

- `[custom]` - 직접 구현한 컴포넌트
- `[@react-three/fiber]` - R3F 코어
- `[@react-three/drei]` - R3F 헬퍼 라이브러리
- `[R3F built-in]` - Three.js 객체

### 2.3 데이터 흐름

```
File Drop → FileUpload → useDXFParser → ParsedCADData → CADMesh → Three.js Render
                              ↓
                        (파일 > 2MB)
                              ↓
                        useDXFWorker (WebWorker)

상태 흐름:
1. File → text() → dxf-parser
2. entities → ParsedLine/Circle/Arc/Polyline
3. cadDataToGeometry() → BufferGeometry
4. <lineSegments> → Three.js 렌더링
```

---

## 3. Implementation Checklist (구현 체크리스트)

### 3.1 Phase 2.1.1: MVP (LINE 엔티티)

- [✅] dxf-parser 패키지 설치
- [✅] CadViewer 폴더 구조 생성
- [✅] 라우트 및 메뉴 아이템 추가 (`/cad-viewer`)
- [✅] types/ 디렉토리 및 constants.ts 작성
- [✅] fileValidator.ts (파일 유효성 검사) - src/utils/로 이동됨
- [✅] dxfToGeometry.ts (DXF→Three.js 변환) 작성
- [✅] useDXFParser.ts 훅 작성
- [✅] FilePanel 컴포넌트 작성 (FileUploadBox, SampleList, UrlInput)
- [✅] CadMesh.tsx 컴포넌트 작성
- [✅] CadScene.tsx 컴포넌트 작성
- [✅] dxf-parser.d.ts 타입 정의 추가
- [✅] 테스트용 샘플 DXF 파일 생성

### 3.2 Phase 2.1.2: 엔티티 확장

- [✅] ParsedCircle, ParsedArc, ParsedPolyline 타입 추가
- [✅] CIRCLE 파싱 및 렌더링 (THREE.EllipseCurve)
- [✅] ARC 파싱 및 렌더링 (degree→radian 변환)
- [✅] POLYLINE/LWPOLYLINE 파싱 및 렌더링 (closed 처리)
- [✅] cadDataToGeometry 통합 함수

### 3.3 Phase 2.1.3: HATCH (2D) 엔티티 ✅

- [✅] ParsedHatch 타입 정의 (`types.ts:79-94`)
- [✅] HATCH 엔티티 파싱 로직 (`useDXFParser.ts:146-220`)
- [✅] HATCH boundary → Three.js Shape 변환 (`dxfToGeometry.ts:289`)
- [✅] Solid Fill 렌더링 (MeshBasicMaterial) (`CadMesh.tsx:275-282`)
- [✅] 렌더링 모드 토글 UI (Wireframe ↔ Solid ↔ Pattern) (`CadScene.tsx:257-273`)
- [✅] Pattern Fill 렌더링 (`dxfToGeometry.ts:383`)
- [✅] 레이어 시스템과 통합 (`CadMesh.tsx:62-67, 310-315`)

### 3.4 Phase 2.1.4: 추가 엔티티 지원 📋

- [ ] ParsedText, ParsedMText 타입 정의 (types.ts)
- [ ] ParsedDimension 타입 정의 (types.ts)
- [ ] ParsedSpline, ParsedEllipse 타입 정의 (types.ts)
- [ ] TEXT/MTEXT 파싱 로직 (useDXFParser.ts)
- [ ] DIMENSION 파싱 로직 (선 + 텍스트 조합)
- [ ] SPLINE 파싱 로직 (베지어 곡선)
- [ ] ELLIPSE 파싱 로직
- [ ] textToGeometry() 변환 함수 (dxfToGeometry.ts)
- [ ] dimensionToGeometry() 변환 함수
- [ ] splineToGeometry() 변환 함수
- [ ] ellipseToGeometry() 변환 함수
- [ ] CadMesh.tsx에 텍스트 렌더링 추가
- [ ] WebWorker에 추가 엔티티 파싱 동기화
- [ ] 추가 엔티티 Unit 테스트

### 3.5 Phase 2.1.5: 레이어 기능 ✅

- [✅] LayerInfo 타입 정의
- [✅] 레이어 파싱 로직 추가
- [✅] LayerPanel UI 컴포넌트
- [✅] 레이어별 렌더링 적용
- [✅] DXF 색상 매핑 (ACI 1-9)

### 3.6 Phase 2.1.6: HATCH 3D Extrusion ⏳

- [ ] hatch3DExtrude.ts 유틸리티 생성 (utils/)
- [ ] ExtrudeGeometry 옵션 정의 (depth, bevelEnabled 등)
- [ ] 2D HATCH boundary → THREE.Shape 변환 로직 확장
- [ ] THREE.ExtrudeGeometry(shape, { depth }) 적용
- [ ] Depth 조절 UI 컴포넌트 (슬라이더 또는 input)
- [ ] 3D HATCH 렌더링 로직 (CadMesh.tsx 확장)
- [ ] 레이어 시스템과 3D HATCH 통합

### 3.7 Phase 2.1.7: ShadingMode 적용 ⏳

- [ ] useCadMaterial.ts 훅 생성 (hooks/)
- [ ] useTeapotMaterial.ts 패턴 참조하여 구현
- [ ] ShadingMode 타입 정의 (wireframe/flat/smooth/glossy)
- [ ] MeshBasicMaterial (wireframe)
- [ ] MeshPhongMaterial (flat - flatShading: true)
- [ ] MeshLambertMaterial (smooth)
- [ ] MeshPhongMaterial (glossy - shininess: 150)
- [ ] CadMesh에 shadingMode prop 추가
- [ ] ShadingSelect UI 컴포넌트 연동
- [ ] 3D 메쉬에만 ShadingMode 적용 (2D 선/면은 기존 유지)

### 3.8 Phase 2.1.8: 성능 최적화 🔄 (3D 확장)

- [✅] 2D Geometry 머징 (mergeBufferGeometries + 메모리 정리)
- [✅] WebWorker 파싱 (대용량 파일 > 2MB)
- [✅] 2D LOD (Level of Detail) - 엔티티 수 기반 자동 세그먼트 조절
- [ ] 3D ExtrudeGeometry 머징 최적화
- [ ] 3D LOD 적용 (ExtrudeGeometry 세그먼트 조절)
- [ ] 3D 메쉬 메모리 관리

### 3.9 Phase 2.1.9: Unit 테스트 🔄 (3D 확장)

- [✅] DXF 파서 유틸리티 테스트 (useDXFParser.test.ts)
- [✅] 지오메트리 변환 로직 테스트 (dxfToGeometry.test.ts)
- [✅] 파일 검증 로직 테스트 (src/utils/fileValidator.test.ts)
- [✅] URL 검증 로직 테스트 (src/utils/urlValidator.test.ts)
- [✅] DXF 로더 훅 테스트 (useDxfLoader.test.ts)
- [✅] 엔티티 파서 테스트 (entityParsers.test.ts)
- [✅] 테스트 커버리지 62% 달성
- [✅] HATCH 2D 파싱 테스트 추가
- [✅] 렌더링 모드 토글 테스트 추가
- [ ] HATCH 3D Extrusion 테스트 (hatch3DExtrude.test.ts)
- [ ] ShadingMode material 테스트 (useCadMaterial.test.ts)
- [ ] 3D 성능 테스트

---

## 4. Key Implementation Details (핵심 구현 상세)

### 4.1 핵심 코드 패턴

#### 타입 정의

```typescript
// types.ts
export interface Point3D {
    x: number;
    y: number;
    z: number;
}

export interface ParsedLine {
    start: Point3D;
    end: Point3D;
    layer?: string;
}

export interface ParsedCircle {
    center: Point3D;
    radius: number;
    layer?: string;
}

export interface ParsedArc {
    center: Point3D;
    radius: number;
    startAngle: number; // degree
    endAngle: number; // degree
    layer?: string;
}

export interface ParsedPolyline {
    vertices: Point3D[];
    closed: boolean;
    layer?: string;
}

// 📋 Phase 2.1.3 - HATCH 엔티티 (계획됨)
export interface ParsedHatch {
    boundaries: Point3D[][]; // 경계 폴리곤 배열 (다중 경계 지원)
    patternName: string; // 'SOLID', 'ANSI31', etc.
    patternScale?: number; // 패턴 스케일
    patternAngle?: number; // 패턴 각도 (degree)
    color?: number; // ACI 색상 인덱스
    layer?: string;
}

export type RenderMode = 'wireframe' | 'solid' | 'pattern';

export interface LayerInfo {
    name: string;
    color: string;
    visible: boolean;
    entityCount: number;
}

export interface ParsedCADData {
    lines: ParsedLine[];
    circles: ParsedCircle[];
    arcs: ParsedArc[];
    polylines: ParsedPolyline[];
    hatches: ParsedHatch[]; // Phase 2.1.3
    // Phase 2.1.4 추가 엔티티
    texts: ParsedText[];
    mtexts: ParsedMText[];
    dimensions: ParsedDimension[];
    splines: ParsedSpline[];
    ellipses: ParsedEllipse[];
    layers: LayerInfo[];
    bounds: BoundingBox;
    metadata: CADMetadata;
    renderMode?: RenderMode;
}

// 📋 Phase 2.1.4 - 추가 엔티티 타입 정의 (계획됨)
export interface ParsedText {
    content: string; // 텍스트 내용
    position: Point3D; // 삽입점
    height: number; // 텍스트 높이
    rotation: number; // 회전 각도 (degree)
    alignment?: 'left' | 'center' | 'right';
    layer?: string;
}

export interface ParsedMText {
    content: string; // 텍스트 내용 (서식 코드 포함 가능)
    position: Point3D; // 삽입점
    height: number; // 텍스트 높이
    width: number; // 텍스트 박스 너비
    rotation: number; // 회전 각도 (degree)
    layer?: string;
}

export interface ParsedDimension {
    dimensionType: 'linear' | 'aligned' | 'angular' | 'radial' | 'diameter';
    defPoint1: Point3D; // 치수선 시작점
    defPoint2: Point3D; // 치수선 끝점
    textPosition: Point3D; // 치수 텍스트 위치
    text?: string; // 치수 텍스트 (자동 계산 또는 오버라이드)
    layer?: string;
}

export interface ParsedSpline {
    controlPoints: Point3D[]; // 제어점 배열
    degree: number; // 차수 (보통 3 = cubic)
    closed: boolean; // 닫힘 여부
    layer?: string;
}

export interface ParsedEllipse {
    center: Point3D; // 중심점
    majorAxisEnd: Point3D; // 장축 끝점 (중심 기준 상대 좌표)
    minorAxisRatio: number; // 단축/장축 비율
    startParam: number; // 시작 파라미터 (0 = 전체 타원)
    endParam: number; // 끝 파라미터 (2π = 전체 타원)
    layer?: string;
}
```

#### 상수 정의

```typescript
// constants.ts
export const FILE_LIMITS = {
    MAX_SIZE_BYTES: 20 * 1024 * 1024, // 20MB
    WARNING_SIZE_BYTES: 5 * 1024 * 1024, // 5MB 경고
    WORKER_THRESHOLD_BYTES: 2 * 1024 * 1024, // 2MB → WebWorker 사용
    ACCEPTED_EXTENSIONS: ['.dxf'],
} as const;

export const LOD_CONFIG = {
    HIGH_QUALITY_SEGMENTS: 64, // 엔티티 < 1000
    MEDIUM_QUALITY_SEGMENTS: 32, // 엔티티 1000-5000
    LOW_QUALITY_SEGMENTS: 16, // 엔티티 > 5000
} as const;

export const DXF_COLOR_MAP: Record<number, string> = {
    1: '#ff0000', // Red
    2: '#ffff00', // Yellow
    3: '#00ff00', // Green
    4: '#00ffff', // Cyan
    5: '#0000ff', // Blue
    6: '#ff00ff', // Magenta
    7: '#ffffff', // White
    8: '#808080', // Gray
    9: '#c0c0c0', // Light Gray
};
```

#### DXF 파싱 훅 패턴

```typescript
// hooks/useDXFParser.ts
export function useDXFParser() {
    const [isLoading, setIsLoading] = useState(false);
    const [error, setError] = useState<string | null>(null);

    const parse = useCallback(async (file: File): Promise<ParsedCADData> => {
        setIsLoading(true);
        setError(null);

        try {
            const text = await file.text();
            const parser = new DxfParser();
            const dxf = parser.parseSync(text);

            const lines = dxf.entities
                .filter((e) => e.type === 'LINE')
                .map((e) => ({
                    start: {
                        x: e.vertices[0].x,
                        y: e.vertices[0].y,
                        z: e.vertices[0].z ?? 0,
                    },
                    end: {
                        x: e.vertices[1].x,
                        y: e.vertices[1].y,
                        z: e.vertices[1].z ?? 0,
                    },
                    layer: e.layer,
                }));

            // ... circles, arcs, polylines 파싱

            return {
                lines,
                circles,
                arcs,
                polylines,
                layers,
                bounds,
                metadata,
            };
        } catch (err) {
            const message = err instanceof Error ? err.message : 'Parse error';
            setError(message);
            throw err;
        } finally {
            setIsLoading(false);
        }
    }, []);

    return { parse, isLoading, error, clearError: () => setError(null) };
}
```

#### 지오메트리 변환 패턴

```typescript
// utils/dxfToGeometry.ts
export function cadDataToGeometry(
    data: ParsedCADData,
    visibleLayers: Set<string>,
    segments?: number
): THREE.BufferGeometry {
    const geometries: THREE.BufferGeometry[] = [];
    const seg = segments ?? getLODSegments(getTotalEntityCount(data));

    // Lines
    geometries.push(linesToGeometry(filterByLayer(data.lines, visibleLayers)));

    // Circles
    geometries.push(
        circlesToGeometry(filterByLayer(data.circles, visibleLayers), seg)
    );

    // Arcs
    geometries.push(
        arcsToGeometry(filterByLayer(data.arcs, visibleLayers), seg)
    );

    // Polylines
    geometries.push(
        polylinesToGeometry(filterByLayer(data.polylines, visibleLayers))
    );

    // Merge all geometries
    const merged = mergeBufferGeometries(
        geometries.filter((g) => g.attributes.position?.count > 0)
    );

    // Cleanup
    geometries.forEach((g) => g.dispose());

    return merged ?? new THREE.BufferGeometry();
}
```

#### CIRCLE → Three.js

```typescript
export function circlesToGeometry(
    circles: ParsedCircle[],
    segments = 64
): THREE.BufferGeometry {
    const vertices: number[] = [];

    for (const circle of circles) {
        const curve = new THREE.EllipseCurve(
            circle.center.x,
            circle.center.y,
            circle.radius,
            circle.radius,
            0,
            Math.PI * 2,
            false,
            0
        );
        const points = curve.getPoints(segments);

        for (let i = 0; i < points.length - 1; i++) {
            vertices.push(points[i].x, points[i].y, circle.center.z);
            vertices.push(points[i + 1].x, points[i + 1].y, circle.center.z);
        }
    }

    const geometry = new THREE.BufferGeometry();
    geometry.setAttribute(
        'position',
        new THREE.Float32BufferAttribute(vertices, 3)
    );
    return geometry;
}
```

#### ARC → Three.js (degree → radian)

```typescript
export function arcsToGeometry(
    arcs: ParsedArc[],
    segments = 64
): THREE.BufferGeometry {
    const vertices: number[] = [];

    for (const arc of arcs) {
        const startRad = (arc.startAngle * Math.PI) / 180;
        let endRad = (arc.endAngle * Math.PI) / 180;
        if (endRad < startRad) endRad += Math.PI * 2;

        const curve = new THREE.EllipseCurve(
            arc.center.x,
            arc.center.y,
            arc.radius,
            arc.radius,
            startRad,
            endRad,
            false,
            0
        );
        // ... points to vertices
    }

    return geometry;
}
```

### 4.2 설정 및 상수

| 상수명                    | 값   | 설명                           |
| ------------------------- | ---- | ------------------------------ |
| `MAX_SIZE_BYTES`          | 20MB | 최대 파일 크기                 |
| `WORKER_THRESHOLD_BYTES`  | 2MB  | WebWorker 사용 기준            |
| `HIGH_QUALITY_SEGMENTS`   | 64   | 원/호 세그먼트 (엔티티 < 1000) |
| `MEDIUM_QUALITY_SEGMENTS` | 32   | 원/호 세그먼트 (1000-5000)     |
| `LOW_QUALITY_SEGMENTS`    | 16   | 원/호 세그먼트 (> 5000)        |

---

## 5. Testing Strategy (테스트 전략)

### 5.1 Unit Tests

**테스트 대상:**

- [✅] `fileValidator.ts` (src/utils/) - 파일 타입/크기 검증
- [✅] `urlValidator.ts` (src/utils/) - URL 검증 및 SSRF 방지
- [✅] `dxfToGeometry.ts` - 각 엔티티 변환 함수
- [✅] `useDXFParser.ts` - 파싱 로직
- [✅] `useDxfLoader.ts` - 파일/URL 로딩 로직
- [✅] `entityParsers.ts` (services/) - 엔티티 파싱 로직

**테스트 파일 위치:**

```
src/features/CadViewer/
├── utils/__tests__/dxfToGeometry.test.ts
├── hooks/__tests__/useDXFParser.test.ts
├── hooks/__tests__/useDxfLoader.test.ts
└── services/__tests__/entityParsers.test.ts

src/utils/__tests__/
├── fileValidator.test.ts
├── urlValidator.test.ts
├── errorClassifier.test.ts
└── errorFormatter.test.ts

tests/mocks/dxf-parser/
├── fixtures.ts
└── mock-config.ts
```

**테스트 예시:**

```typescript
describe('linesToGeometry', () => {
    it('should convert ParsedLine[] to BufferGeometry', () => {
        const lines: ParsedLine[] = [
            { start: { x: 0, y: 0, z: 0 }, end: { x: 10, y: 0, z: 0 } },
        ];
        const geometry = linesToGeometry(lines);
        expect(geometry.attributes.position.count).toBe(2);
    });

    it('should handle empty array', () => {
        const geometry = linesToGeometry([]);
        expect(geometry.attributes.position.count).toBe(0);
    });
});
```

### 5.2 Integration Tests

- [ ] 파일 업로드 → 렌더링 E2E 테스트
- [ ] 레이어 토글 기능 테스트
- [ ] WebWorker 파싱 테스트

### 5.3 검증 체크리스트

| 검증 항목                       | 방법                 | 상태 |
| ------------------------------- | -------------------- | ---- |
| DXF 파일 업로드                 | 수동 확인            | ✅   |
| LINE/CIRCLE/ARC/POLYLINE 렌더링 | 수동 확인            | ✅   |
| 레이어 토글                     | 수동 확인            | ✅   |
| OrbitControls 조작              | 수동 확인            | ✅   |
| 파일 크기 제한 (20MB)           | 수동 확인            | ✅   |
| WebWorker (> 2MB)               | 수동 확인            | ✅   |
| 타입 체크                       | `npm run type-check` | ✅   |
| 린트                            | `npm run lint`       | ✅   |
| 단위 테스트                     | `npm run test`       | ✅   |

---

## 6. Routes & Navigation (라우트 및 네비게이션)

| Path          | Component     | Description          |
| ------------- | ------------- | -------------------- |
| `/cad-viewer` | CADViewerPage | CAD 뷰어 메인 페이지 |

---

## 7. Dependencies & References (의존성 및 참조)

### 7.1 필수 패키지

| 패키지               | 버전     | 용도                  |
| -------------------- | -------- | --------------------- |
| `dxf-parser`         | ^1.1.2   | DXF 파일 파싱 (ASCII) |
| `three`              | ^0.181.0 | 3D 렌더링             |
| `@react-three/fiber` | ^8.x     | React Three.js 통합   |
| `@react-three/drei`  | ^9.x     | OrbitControls 등      |

### 7.2 참조 문서

| 문서                                                            | 역할               |
| --------------------------------------------------------------- | ------------------ |
| [Phase 1.2 Teapot](../01-Foundation/1.2_THREEJS_DEMO_TEAPOT.md) | 컴포넌트 패턴 참조 |
| [DEV_GUIDE.md](../../DEV_GUIDE.md)                              | 개발 컨벤션        |
| [ARCHITECTURE.md](../../ARCHITECTURE.md)                        | 시스템 아키텍처    |

### 7.3 관련 Phase

| Phase     | 관계 | 설명                     |
| --------- | ---- | ------------------------ |
| Phase 1.2 | 의존 | Teapot 패턴 완료 후 시작 |
| Phase 2.2 | 후속 | PDF 뷰어 (백엔드 필요)   |
| Phase 3   | 연관 | 백엔드 API 연동          |

---

## 8. Usage Guide (사용 가이드)

### 8.1 개발 서버 실행

```bash
npm run dev
```

### 8.2 브라우저 접속

```
http://localhost:3000/cad-viewer
```

### 8.3 사용법

1. 좌측 상단 업로드 영역에 DXF 파일 드래그 앤 드롭
2. 또는 클릭하여 파일 선택
3. 3D 와이어프레임 렌더링 확인
4. 마우스로 회전/확대/이동 조작
5. 우측 레이어 패널에서 레이어별 표시/숨김

---

## 9. Post-Completion Checklist (완료 후 체크리스트)

개발 완료 시 Claude가 아래 문서들의 업데이트를 자동으로 제안합니다.

### 9.1 연관 문서 업데이트 (Claude 자동 제안)

| 문서                             | 업데이트 내용                                     |
| -------------------------------- | ------------------------------------------------- |
| `docs/phases/PHASE_NAV_GUIDE.md` | Phase 문서 링크, 상태, 진행률 업데이트            |
| `docs/ROADMAP.md`                | Phase 상태 (🔄→✅), 진행률, Milestones, 변경 이력 |
| `docs/GLOSSARY.md`               | 새로운 용어/약어 추가                             |
| `docs/ARCHITECTURE.md`           | 패키지 구조, 레이어별 역할, 관련 문서 링크        |
| `README.md`                      | 현재 상태, 구현 완료 기능, 개발 예정 목록         |

### 9.2 업데이트 프로세스

1. **구현 완료** → Claude가 "연관 문서 업데이트할까요?" 제안
2. **변경 내용 미리보기** → 각 문서별 수정될 내용 표시
3. **승인** → 사용자 확인 후 일괄 업데이트

---

## 10. Changelog (변경 이력)

| 버전  | 날짜       | 변경 내용                                                                                                                                                                                               |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.0.6 | 2025-12-29 | 코드 동기화: types/ 구조화, workers/→services/, validators→src/utils 이동, FileUpload→FilePanel, 새 훅(useDxfLoader) 및 서비스 파일 반영, Phase 1.2 링크 수정 (1.2_TEAPOT_DEMO→1.2_THREEJS_DEMO_TEAPOT) |
| 0.0.5 | 2025-12-22 | 마일스톤 재배치: 2.1.6 HATCH 3D, 2.1.7 ShadingMode 추가, 2.1.8 성능최적화/2.1.9 Unit테스트 3D 확장으로 이동, ROADMAP.md 동기화, 진행률 60%                                                              |
| 0.0.4 | 2025-12-17 | Phase 2.1.4 추가 엔티티 계획 추가 및 번호 재정렬, HATCH 구현 완료, Claude Commands 섹션 추가, 타입 정의 추가                                                                                            |
| 0.0.3 | 2025-12-04 | 템플릿 v0.0.2 기준 섹션 순서 변경 (Routes↔Dependencies)                                                                                                                                                 |
| 0.0.2 | 2025-12-03 | Phase 2.1 완료: 단위 테스트 구현 (58개 통과), 템플릿 기준 메타데이터 간소화, utils 커버리지 98.29% 달성                                                                                                 |
| 0.0.1 | 2025-12-02 | 템플릿 적용, 버전 가이드라인 적용 (0.0.x), Section 10 추가                                                                                                                                              |
| 0.0.0 | 2025-12-01 | 초기 문서 작성, 2.1.1~2.1.4 구현 완료                                                                                                                                                                   |
